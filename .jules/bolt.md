## 2026-09-01 - [O(N^2 log N) 렌더링 최적화]
**학습 내용:** [배열 순회 중 원본 배열을 복사 및 정렬(sort)하는 작업이 포함되면 성능에 치명적임을 확인했습니다. O(N^2 log N)에서 O(N log N)으로 복잡도를 줄이면서, 시간 소요를 442ms에서 2ms로 크게 개선했습니다.]
**적용 계획:** [반복문 내부에 O(N) 이상의 작업(정렬, 필터링, 대규모 복사 등)이 포함된 경우 외부로 분리하고, O(1) 탐색이 가능한 Map 등을 활용하는 패턴을 우선 적용할 것입니다.]